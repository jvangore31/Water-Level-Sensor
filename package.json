{
  "name": "esp32-water-level-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Live ESP32 water level monitor with a USB bridge and web dashboard",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "start": "node scripts/dev.mjs",
    "start:bridge": "npm --prefix bridge start",
    "start:dashboard": "npm --prefix dashboard run dev",
    "build": "npm --prefix dashboard run build",
    "build:firmware-assets": "node scripts/embed-dashboard.mjs",
    "test": "npm --prefix bridge run check && npm --prefix dashboard run lint && npm --prefix dashboard run build"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
